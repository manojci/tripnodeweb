form(id="search", method="POST", action="/search")
	lable(for='origin') From
	input#origin(type='text', name='origin')
	lable(for='desitination') To
	input#desitination(type='text', name='desitination')
	input(type="submit", value="Search")

h1!=message